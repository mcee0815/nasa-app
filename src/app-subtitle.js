
import React from 'react';

const AppSubTitle = ({subtitle}) => {
    return (
      <div className="col-10 mx-auto">
        <h6 className="mb-2 text-muted">{subtitle}</h6>
      </div>
    );
  }

  export default AppSubTitle;