
import React from 'react';

const AppTitle = ({appTitle}) => {
    return (

      <div className="col-10 mx-auto">
        <h2 className="text-center text-white mt-4 mb-2">{appTitle}</h2>
      </div>
    );
  }

  export default AppTitle;